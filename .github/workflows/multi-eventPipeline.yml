name: "Multi event Pipeline Workflow"

on:
    push:
        branches:
            - master
            - test
        
jobs:
    on-push:
        runs-on: ubuntu-latest
        steps:
            - name: "Running on master branch push"
              run: echo "This job runs on push to master branch"

